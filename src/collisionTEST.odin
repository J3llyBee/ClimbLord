package main

// // testing stuff doesn't work at all
// import "core:fmt"

// import rl "vendor:raylib"

// predict_col :: proc(using e: ^Entity, vel: vec2 ) {
// 	rect: = rl.Rectangle {pos.x, pos.y - size.y / 2, vel.x, size.y}
// 	// check for collisions in the rect
// }